const validateBody = require("./validateBody");
const isValidId = require("./isValidid");
const authenticate = require("./aythent");

module.exports = {
  validateBody,
  isValidId,
  authenticate,
};